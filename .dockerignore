# ingest/, api/, and search/ build with context "." (repo root) so their
# Dockerfiles can also COPY proto/. Keep that context lean.
.git/
agent/target/
search/target/
web/node_modules/
web/build/
web/.svelte-kit/
hack/dev-certs/out/
